#include "main.h"
/**
 * infinite_add - adds two numbers
 *@n1: first number
 *@n2: second number
 *@r: buffer for result
 *size_r: buffer size
 * Return: address of r or 0
 */
char
{
int;
for ()
for ()
if ()
return (0);
m = 0;
for ()
{
n = m;
if (i >= 0)
  n += n1
    if
      }
}
