#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
(*
 * Return: 0 on success
 */
int main(void)
{
	int n;
  
  for (i = 97; i < 123; i++)
  if (i != 101 && i != 113)
  putchar(i);
  putchar('\n');
  return (0);
}
