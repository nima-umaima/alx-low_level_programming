#include "main.h"
#include <stdio.h>
/**
 * _abs - Compute the absolute value of an integer
 *@n: Number being tested
 * Return: Always 0
 */
int _abt(int n)
{
  if(n < 0)
    {
      return (-n);
    }
  else
    {
      return (n);
    }
  return (0);
}
