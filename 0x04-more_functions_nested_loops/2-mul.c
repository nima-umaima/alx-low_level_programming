#include "main.h"
#include <stdio.h>
/**
 * mul - Multiply 2 numbers
 *@a: number being tested
 *@B: number being tested
 * Return: Always 0
 */
int mul(int a, int b)
{
int product;
product = a * b;
return (product);
}
