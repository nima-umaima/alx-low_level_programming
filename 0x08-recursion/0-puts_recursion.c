#include "main.h"
/**
 * _puts_recursion - create put fun
 *@s: string
 * Return: string
 */
void _puts_recursion(char *s)
{
  if ()
    {
      _putchar('\n');
    }
  else
    {
      _putchar(*s);
      _putchar(s + 1);
    }
}
